apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: testcoreapp
  labels:
        app: testcoreapp
        env: production
        track: stable
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: testcoreapp
        env: production
        track: stable
    spec:
      containers:
      - name: testcoreapp
        image: {{image}}
        ports:
        - name: tca-port
          containerPort: 80
