apiVersion: v1
kind: Service
metadata:
  labels:
    env: production
    app: testcoreapp
    track: canary
  name: testcoreapp-canary
  namespace: production
spec:
  externalTrafficPolicy: Cluster
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    env: production
    app: testcoreapp
    track: canary
  type: NodePort
