apiVersion: v1
kind: Service
metadata:
  labels:
    env: production
    app: testcoreapp
    type: canary
  name: testcoreapp-canary
  namespace: production
spec:
  externalTrafficPolicy: Cluster
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: testcoreapp
    type: canary
  type: NodePort
